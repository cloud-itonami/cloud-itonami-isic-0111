{:paths ["src"]
 ;; langgraph brings langchain transitively (both zero-/single-dep,
 ;; all .cljc -> portable to JVM / SCI / ClojureScript / GraalVM). It lives
 ;; in the kotoba-lang org of this workspace (transferred + renamed from
 ;; com-junkawasaki/langgraph-clj / com-junkawasaki/langchain-clj). This
 ;; repo is standalone (forkable outside the workspace); the :local/root
 ;; paths only resolve inside the monorepo checkout -- a standalone fork
 ;; should override them with git coordinates.
 :deps {io.github.kotoba-lang/langgraph
        {:local/root "../../kotoba-lang/langgraph"}
        ;; langchain-store: the shared langchain.db store seam (EDN-blob
        ;; codec + identity schema + event-log read/append helpers) that
        ;; ~190 cloud-itonami actors hand-roll (ADR-2607141600);
        ;; cerealops.store's DatomicStore uses it instead of its own
        ;; enc/dec* two-liner.
        io.github.kotoba-lang/langchain-store
        {:local/root "../../kotoba-lang/langchain-store"}
        ;; The HOST half of this actor, shared with the marketplace family
        ;; rather than reimplemented. Its own docstring says it is written
        ;; "for any actor"; this is the first non-marketplace actor to use
        ;; it. cloud-itonami-isic-6492 took the other road and carries 1,229
        ;; lines of edge, 1,003 of them generic.
        io.github.kotoba-lang/marketplace
        {:local/root "../../kotoba-lang/marketplace"}
        ;; kotobase.net client, for the host half only (cerealops.edge.*):
        ;; q / pull / datoms / transact under a per-request CACAO.
        io.github.kotoba-lang/kotobase-client
        {:local/root "../../kotoba-lang/kotobase-client"}}
 :aliases
 {;; the Worker build's classpath (shadow-cljs :deps {:aliases [:cljs]})
  :cljs {:extra-deps {thheller/shadow-cljs {:mvn/version "2.28.20"}}}
  ;; offline workspace dev -- pin transitive langchain to local checkout
  :dev {:override-deps
        {io.github.kotoba-lang/langchain
         {:local/root "../../kotoba-lang/langchain"}}}
  ;; clojure -M:dev:run  -> drive the demo through one OperationActor
  :run {:main-opts ["-m" "cerealops.sim"]}
  :render-html {:main-opts ["-m" "cerealops.render-html"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; static analysis -- clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
