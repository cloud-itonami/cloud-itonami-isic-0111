{
  "name": "cloud-itonami-isic-0111",
  "private": true,
  "comment": "shadow-cljs compiles src/cerealops/edge/worker.cljs -> dist/worker.js, the kotobase host for the cereal-ops actor. Same dependency set as cloud-itonami-marketplace-order because it uses the same shared host.",
  "scripts": {
    "build": "shadow-cljs release worker",
    "deploy": "npm run build && wrangler deploy --dispatch-namespace ai-gftd-repository-dispatch"
  },
  "devDependencies": {
    "@noble/hashes": "^2.2.0",
    "shadow-cljs": "^2.28.20",
    "wrangler": "^4.0.0",
    "@noble/curves": "^1.6.0",
    "@ipld/dag-cbor": "^9.2.1"
  }
}
